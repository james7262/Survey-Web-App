type Respondents @model @auth(rules: [{allow: public}]) {
  id: ID!
  firstName: String!
  lastName: String!
  emailAddress: String!
}

type Survey @model @auth(rules: [{allow: public}]) {
  id: ID!
  SurveyNam: String!
  NumberOfQuestions: Int!
}
